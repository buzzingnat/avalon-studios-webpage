{
  "name": "avalon-studios-webpage",
  "version": "1.0.0",
  "description": "Game studio website for Avalon Studios",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pug-local": "node_modules/pug-cli",
    "css-build": "sass src/sass/style.scss dist/style.css",
    "css-watch": "npm run css-build -- --watch",
    "start-sass": "npm run css-watch",
    "start-pug": "pug -w ./src -o ./dist -P",
    "start": "npm run start-pug & npm run css-watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bulma": "^1.0.1",
    "sass": "^1.64.1"
  }
}
